The DTOs folder contains the classes whose instances will be de/serialized for web request/response bodies.

The ORM folder contains the classes used by Entity Framework to map db rows to class instances.

The Adapter folder contains the classes used to translate a DTO instance to an ORM instance, and vice versa.